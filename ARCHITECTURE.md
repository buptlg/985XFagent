# 架构设计 — 高考志愿犀利助手(working name:报吧 / XFagent)

> 形态:**全部网页端(做漂亮 + 移动端响应式,手机浏览器即用,不做小程序)**。后端 Node + 自部署开源模型(OpenAI 兼容,mock 兜底)。
> 数据层:**混合**——锚点案例挂硬数据,其余定性锐评。
> 张雪峰:**双人设可切换**(默认致敬版;扮演版明确标注"逝者方法论风格复刻·致敬")。
> ⚠️ 张雪峰已于 2026-03-24 去世(心源性猝死)。扮演版不得宣称在世、不得伪造其新发言。

---

## 一、模式(Modes)

### 1) 985吧·实时锐评模式 ⭐(产品大头)
让用户感到"**当下、犀利、抓眼球**"。子功能:
- **今日锐评流**:卡片信息流,按主题(专业鄙视链/院校battle/分数线跳水/招生套路)滚动展示最热+最新+最尖的 985吧 锐评(取自 corpus,带发帖时间+热度+原帖金句)。
- **招生风格图鉴**:把"超短裙/丁字裤/姜-泰勒/共轭"等可视化,点名各校招生套路(爆款入口)。
- **黑话解码**:点词即弹释义(超短裙/天坑/文工团/宇宙机/WIN…),解决"小白看不懂"。
- **毒舌问答**:用户问报志愿 → 以 985吧 集体智慧的犀利口吻作答,挂语料+硬数据,黑话自动解释。
- **避坑雷达**:输入院校/专业 → 标出超短裙藏分、综评藏天坑、护校水军等坑。

### 2) 张雪峰·方法论模式
- **致敬版(默认)**:用张雪峰方法论框架(先专业→次城市→后院校;理工看专业/医学看地域/文科看学校;保研率正相关…),第三人称"张老师的思路是…"。
- **扮演版(可切换)**:第一人称口吻复刻,顶部固定 banner:"本模式为已故张雪峰老师方法论风格的致敬性复刻,非本人,不代表其真实观点"。

### 3) 融合模式(985吧 × 张雪峰)
- 张雪峰框架打底(结构化决策)+ 985吧 实时锐评当"弹药/补刀"。一句话:**框架 + 当下炮火**。

### 4) 数据增强(与其他信息结合)
- 任何模式作答时,锚点结论自动挂硬数据(见 `data/anchors.json`),并附"以各校官方最新为准"声明。

---

## 二、数据层(混合)

```
data/
  anchors.json     # 硬数据锚点:案例→数字+来源+年份(临床跳水/哈工尖班/计算机寒冬十年线…)
  heihua.json      # 黑话词典(结构化,供"黑话解码")
  modes.json       # 模式定义与系统提示模板
corpus/            # 原始锐评语料(markdown,人读+喂模型)
  00-985吧锐评总汇.md
  01..03 三主线
  04-985吧黑话与方法论.md
```

**锚点硬数据(首批)**:
- 临床跳水:华科临八陕西位次 23前<400 → 24年800 → 25年1800;郑大河南临床进调剂。
- 哈工尖班 vs 华五:河南 哈工693(尖班)>中科大692;江苏 哈工本部平均666.5>南大/人大665。
- 21年对标:早年哈工大"不玩丁字裤招生"时分数≈山大/吉大。
- 计算机寒冬十年线:2015→2026"寒冬"年年喊,宇宙机未真凉(语料佐证)。
- "实时性"诚实声明:贴吧实时抓取脆弱 → 用**定期刷新的语料快照 + 采集时间戳**呈现,不谎称秒级实时。

---

## 三、技术栈

- `web/` — **Vite + React + TS + Tailwind**(重点做漂亮)。信息流卡片 + 对话。
- ~~`miniprogram/` 微信小程序~~ —— **已砍**(用户定:全部网页端)。移动端改为 **web 响应式**,手机浏览器直接用。
- `server/` — **Node**;`/api/chat` 按 mode 组装 system prompt(读 `data/modes.json`,注入 corpus 金句/anchors/heihua)。**引擎=自部署开源模型**,OpenAI 兼容端点(dev=Ollama `http://localhost:11434/v1`,prod=vLLM)。env:`LLM_BASE_URL`/`LLM_MODEL`(默认 `qwen2.5:14b`)。**不依赖外部 key**;模型未起时 mock 兜底。
- `shared/` — 模式定义、提示模板、数据 schema。
- 现状:模型未起 → server **mock 兜底**(基于语料拼装答案)供前端开发/测试;`ollama pull qwen2.5:14b` 起来后自动走真模型。显存建议 14B 起(7B 偏通用、"没灵魂")。

---

## 四、合规 / 调性红线

- 张雪峰去世为事实,扮演版必须带致敬声明,不伪造发言。
- 锐评含社区情绪,作答**区分 事实/社区情绪/个人选择**三层。
- 涉具体个人的未证实负面指控不纳入。
- 涉及离世/心理健康的语料,严肃呈现,不娱乐化。
- 一切分数/政策结论附"以官方最新为准"。

---

## 五、里程碑

1. ✅ 语料库 + 黑话 + 架构
2. ✅ web(Vite+Tailwind v4)+ 985吧 today-feed 页(**真实金句**,非假数据)+ 黑话解码抽屉 + 张雪峰双人设 + 硬数据锚点 + 融合页 —— localhost:5173 已验证漂亮
3. ✅ 985吧采集批次1-5(临床/哈工尖班/超短裙/后悔/金融跌落/选调就业)
4. ✅ 招生风格图鉴(超短裙/姜-泰勒/共轭 可视化,985 mode 内)
5. ✅ server(Node,OpenAI兼容→Ollama/vLLM,无外部key,mock兜底,检索注入52条语料)+ data/modes.json + /api/chat + 前端『毒舌问答』(vite proxy,端到端实测通过)
6. ✅ 避坑雷达(院校/专业→坑,接就业硬数据 anchors)
7. ✅ 移动端响应式(导航 sub 移动端隐藏 / 边距 px-4 / 网格手机1列,代码层完成;视觉核对待用户空时)
8. ✅ 测试用例(`tests/runCases.mjs` 5 考生 case → `tests/cases.md`)+ 检索升级(2-gram,修"山东→山大"误命中)
9. ✅ mock 按人设区分语气 + 控长度(四人设实测分化)
10. ✅ 对话知识库统一为 `web/src/data/feed.json`(干净金句;改 feed.ts 后跑 `node scripts/feedToJson.mjs` 同步)
11. 🔄 UI 打磨:✅ favicon(红底报)/✅ 🎲随机毒舌聚光/✅ 生产构建通过(tsc -b && vite build)/✅ README;⏳ 加载骨架/微动效
12. ⏳ 内容补:法学/文科向/保研/北邮(待用户"再扒")
13. ✅ 接入真实 LLM:server 复用系统 `OPENAI_*`(api.gpt.ge / gpt-5.4-high),超时放宽 240s;实测 engine=llm,三层结构+语料引用+黑话解释,约 77s/条。密钥仅从环境读取,不落文件。
