# 对话引擎配置 —— 复制本文件为 .env 并填你的值。
# ⚠️ .env 不会进 git;API key 别贴进聊天/截图/提交里。

# 提示:若系统已设 OPENAI_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL 环境变量,
#       server 会自动复用,本文件可不建。(推理模型如 gpt-5.x-high 较慢,约1分钟/条;
#       想快可把 OPENAI_MODEL 设成更快的非推理模型)

# 默认(不接 key):本地 Ollama,这三行留空即可
# LLM_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=qwen2.5:14b

# ===== 用云端 API(OpenAI 兼容,即插即用)时取消注释并填写 =====
# DeepSeek(推荐·便宜·中文强):
# LLM_BASE_URL=https://api.deepseek.com/v1
# LLM_MODEL=deepseek-chat
# LLM_API_KEY=sk-在这里填你自己的key

# OpenAI:           base=https://api.openai.com/v1                       model=gpt-4o-mini
# Kimi/Moonshot:    base=https://api.moonshot.cn/v1                      model=moonshot-v1-8k
# 智谱 GLM:         base=https://open.bigmodel.cn/api/paas/v4            model=glm-4
# 通义 Qwen(兼容):  base=https://dashscope.aliyuncs.com/compatible-mode/v1  model=qwen-plus
#
# 注:Anthropic(Claude)不是 OpenAI 兼容格式,需要我加一个适配器 —— 告诉我即可。
